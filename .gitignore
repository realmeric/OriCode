# Generated by XcodeGen, and iCloud makes numbered copies of it in Documents.
*.xcodeproj/
build/
engine/node_modules/
.DS_Store
xcuserdata/
# A screenshot of another company's app, kept as a local design reference.
docs/reference.png
# Kept on this Mac only, in Documents, which iCloud Drive syncs: the plan with every card's
# Notes, the instructions agents work it by, and the agents' worktrees and settings.
KANBAN.md
CLAUDE.md
.claude/
